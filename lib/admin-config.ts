// Single source of truth for admin access control.
// Importable from both server (auth-helpers) and client components (admin pages).
export const OWNER_ADMIN_EMAIL = 'secondabenjamin.2000@gmail.com'
